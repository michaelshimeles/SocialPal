"use client"

import { Button } from "@/components/ui/button";

const Pillars = ({ }) => {
    return (
        <div>
            <Button>Create Content Pillars</Button>
        </div>
    );
}

export default Pillars;